apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: skillfybank
  labels:
    app: account-service
spec:
  type: NodePort
  selector:
    app: account-service
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30000
